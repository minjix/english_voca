# 프로젝트: 카톡 영단어 알림 (일일 회화 단어 5개)

## 목적
매일 오전 8시30분에 "회화에서 자주 쓰이는 영단어" 5개를 뜻과 함께 뽑아서
내 카카오톡(나에게 보내기)으로 전송하는 개인용 자동화 프로그램.
- 사용자는 나 혼자 (개인 프로젝트). 로그인/회원가입/웹 UI 불필요.
- 결과물은 카카오톡 메시지 하나. 별도의 프론트엔드(React 등)는 필요 없음 — 화면을 볼 필요가 없기 때문.
- 포맷은 크게 중요하지 않음(단어, 의미/뜻이 잘 보이면 됨)

## 기술 스택 결정
- **언어**: Java 
- **구조**: 단일 실행 단위(배치 잡). 웹 서버/Spring Boot 백엔드는 두지 않는다 — 상시 서버가 필요 없는 작업이므로 과한 구성.
- **빌드**: Gradle
- **실행 환경**: 현재 로컬에서 진행

## 단어 소스
- "회화에서 자주 쓰이는 단어"인지 여부는 사전 API만으로는 판별이 안 됨 (사전 API는 뜻/예문 조회용).
  → 별도로 유지하는 **단어 후보 목록**(Oxford 3000/5000, NGSL 같은 공개 빈도 리스트 활용)을 CSV/JSON으로 프로젝트에 내장하고, 이 목록에서 매일 5개를 뽑는다.
- 각 단어의 **뜻**은 [MyMemory Translation API](https://mymemory.translated.net/doc/spec.php)로 조회: 단어 자체를 영→한(`langpair=en|ko`)으로 직접 번역해 그 결과를 뜻으로 사용 (API 키 불필요, 무료)
  - 예문(example)은 더 이상 사용하지 않음
  - 번역 실패(HTTP 오류, 빈 응답 등) 시 3회 재시도 후에도 실패하면 해당 단어는 스킵하고 다음 단어로 대체

## 중복 방지 (상태 저장)
- 상시 서버/DB가 없으므로 최소 구성으로 상태를 저장해야 함:
  - S3에 작은 JSON 파일 하나를 두고 "마지막으로 사용한 단어 목록 인덱스" 또는 "이미 보낸 단어 목록"을 기록/조회
- 목록을 다 소진하면 처음부터 다시 순환

## 카카오톡 전송
- **카카오 "나에게 보내기" API** 사용 — 개인 계정으로 본인에게 메시지를 보내는 무료 API
  1. Kakao Developers에서 앱 등록 → REST API 키 발급
  2. 최초 1회 OAuth 인가 코드 플로우로 access token / refresh token 발급 (수동으로 1회 진행)
  3. 이후 Lambda 실행 시마다: refresh token으로 access token 갱신 → "나에게 보내기" 메시지 API 호출
- **주의사항**
  - access token 만료: 약 6시간 / refresh token 만료: 약 2개월 → 매일 자동 갱신되는 코드 필요, 갱신 시 refresh token 자체도 갱신될 수 있으니 최신 값으로 계속 교체 저장할 것
  - REST API 키, access/refresh token은 코드에 하드코딩 금지. AWS Secrets Manager 또는 Parameter Store(SecureString)에 저장하고 Lambda에서 조회
  - 메시지는 기본 텍스트 템플릿 사용, 글자 수 제한 확인 후 포맷 구성 (단어 5개 + 뜻을 한 메시지에 정리)

## 코딩 컨벤션
- 개인용 소규모 배치 프로그램이므로 과도한 레이어 분리 지양 (단일 모듈/클래스 몇 개로 시작)
- 민감정보(API 키, 토큰)는 절대 커밋하지 않음 — `.gitignore`로 로컬 설정/시크릿 파일 제외
- 외부 호출(사전 API, 카카오 API) 실패 시 재시도/에러 로깅 정책을 최소한으로라도 넣기 (Lambda 실패 시 알림 없으면 며칠간 조용히 안 올 수 있음)


## 참고: 실행 전 계획 확인이 필요하다면
다른 프로젝트에서처럼 "코드 작성 전 계획을 먼저 제시하고 승인받는" 방식을 원하면 아래 규칙을 추가:
- 코드를 작성/수정하기 전에 항상 계획을 먼저 제시하고 승인을 받을 것
- 계획은 `.plans/` 폴더에 날짜별 `.md` 파일로 저장할 것